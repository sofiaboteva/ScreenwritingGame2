
export const appContext = {
  CENTRAL_WIDTH: 400,
  CENTRAL_HEIGHT: window.innerHeight,
  GAME_ZONE_PADDING: 24,
  PADDING: 24,
  BUTTON_WIDTH: window.innerWidth * 0.75,
  BUTTON_HEIGHT:  window.innerHeight * 0.15,
  BUTTON_SPACING:  window.innerHeight * 0.01,
  LABEL_VERTICAL_SPACING:  window.innerWidth * 0.25,
  TEXT_SIZE: window.innerWidth * 0.05,
};
